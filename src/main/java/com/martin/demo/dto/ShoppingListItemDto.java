/*package com.martin.demo.dto;

import com.martin.demo.ShoppingListItem;
import java.math.BigDecimal;
import java.time.Instant;

public class ShoppingListItemDto {
    private Long id;
    private String name;
    private BigDecimal quantity;
    private String unit;
    private boolean bought;
    private Instant boughtAt;
    private int position;
    private String note;

    public ShoppingListItemDto(ShoppingListItem item) {
        this.id = item.getId();
        this.name = item.getName();
        this.quantity = item.getQuantity();
        this.unit = item.getUnit();
        this.bought = item.isBought();
        this.boughtAt = item.getBoughtAt();
        this.position = item.getPosition();
        this.note = item.getNote();
    }

    public Long getId() { return id; }
    public String getName() { return name; }
    public BigDecimal getQuantity() { return quantity; }
    public String getUnit() { return unit; }
    public boolean isBought() { return bought; }
    public Instant getBoughtAt() { return boughtAt; }
    public int getPosition() { return position; }
    public String getNote() { return note; }
}
*/
